_target_: src.data.drugbank_module.DrugbankDataModule

num_workers: 0
pin_memory: False
batch_size: 64 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)

data_cfg:
  metadata_path: "data/raw/sample.csv"
  k: 5 # number of nearest neighbours considered when creating graph adjacency